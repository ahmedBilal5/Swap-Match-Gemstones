#ifndef GEM_H
#define GEM_H

class Gem{
        int type;
        public:
                Gem();
                Gem(int t);
                void setType(int t);
                int getType();
};
#endif

